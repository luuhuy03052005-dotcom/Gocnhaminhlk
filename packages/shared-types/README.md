# Shared Types

This folder is for shared TypeScript DTOs and model contracts.

MVP may keep this simple.

Recommended future files:

```txt
user.types.ts
menu.types.ts
voucher.types.ts
order.types.ts
notification.types.ts
feature-flag.types.ts
api-response.types.ts
```

Do not create conflicting field names.

Use:

```ts
fullName
phoneNumber
firebaseUid
imageUrl
createdAt
updatedAt
```
