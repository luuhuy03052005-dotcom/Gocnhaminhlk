<!DOCTYPE html>
<html lang="vi">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Bamos Coffee — Layout Chuẩn</title>
<style>
  /* ── RESET & BASE ── */
  *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }
  :root {
    --brand:     #C8873A;
    --brand-dk:  #A06828;
    --text:      #2C2017;
    --muted:     #7A6A55;
    --bg:        #FDF6EE;
    --bg-alt:    #F5EDE0;
    --white:     #FFFFFF;
    --radius:    12px;
    --gap:       24px;
    --max:       1200px;
    --pad:       clamp(16px, 4vw, 40px);

    --fs-xs:     12px;
    --fs-sm:     14px;
    --fs-base:   16px;
    --fs-lg:     18px;
    --fs-xl:     24px;
    --fs-2xl:    32px;
    --fs-3xl:    clamp(36px, 5vw, 56px);
  }
  body { font-family: 'Segoe UI', sans-serif; font-size: var(--fs-base);
         color: var(--text); background: var(--bg); line-height: 1.6; }

  /* ── CONTAINER ── */
  .container { max-width: var(--max); margin-inline: auto; padding-inline: var(--pad); }

  /* ── 12-COL GRID ── */
  .grid-12 { display: grid; grid-template-columns: repeat(12, 1fr); gap: var(--gap); }
  .col-4  { grid-column: span 4; }
  .col-5  { grid-column: span 5; }
  .col-6  { grid-column: span 6; }
  .col-7  { grid-column: span 7; }
  .col-8  { grid-column: span 8; }
  .col-12 { grid-column: span 12; }

  /* ── TYPOGRAPHY ── */
  .eyebrow { font-size: var(--fs-sm); color: var(--brand); letter-spacing: .12em;
             text-transform: uppercase; font-weight: 600; margin-bottom: 8px; }
  .h1 { font-size: var(--fs-3xl); font-weight: 800; line-height: 1.15;
        color: var(--text); max-width: 800px; }
  .h2 { font-size: var(--fs-2xl); font-weight: 700; line-height: 1.2; }
  .h3 { font-size: var(--fs-xl); font-weight: 700; }
  .subtext { font-size: var(--fs-lg); color: var(--muted); max-width: 640px; line-height: 1.5; }
  .label { font-size: var(--fs-xs); font-weight: 700; letter-spacing: .1em;
           text-transform: uppercase; }

  /* ── BUTTONS ── */
  .btn { display: inline-flex; align-items: center; gap: 8px; padding: 14px 28px;
         border-radius: 8px; font-weight: 700; font-size: var(--fs-base);
         cursor: pointer; border: 2px solid transparent; transition: all .2s; }
  .btn-primary { background: var(--brand); color: #fff; }
  .btn-primary:hover { background: var(--brand-dk); }
  .btn-ghost   { background: transparent; color: var(--brand); border-color: var(--brand); }
  .btn-ghost:hover { background: var(--brand); color: #fff; }

  /* ── SECTION SPACING ── */
  section { padding-block: clamp(48px, 8vw, 96px); }

  /* ── PLACEHOLDER VISUAL ── */
  .img-ph { background: #DDD5C8; border-radius: var(--radius); width: 100%; }
  .img-16-9  { aspect-ratio: 16/9; }
  .img-4-5   { aspect-ratio: 4/5; }
  .img-1-1   { aspect-ratio: 1/1; }
  .img-3-4   { aspect-ratio: 3/4; }
  .img-ph span { display: grid; place-items: center; height: 100%;
                 font-size: var(--fs-sm); color: var(--muted); }

  /* ══════════════════════════════════════════════
     HEADER
  ══════════════════════════════════════════════ */
  header {
    position: sticky; top: 0; z-index: 100;
    background: var(--white); border-bottom: 1px solid #EDE4D8;
    box-shadow: 0 2px 8px rgba(0,0,0,.06);
  }
  .header-inner {
    display: flex; align-items: center; justify-content: space-between;
    height: 68px;
  }
  .logo { font-size: var(--fs-xl); font-weight: 900; color: var(--brand);
          letter-spacing: -.02em; }
  .logo span { color: var(--text); }
  nav { display: flex; gap: 4px; }
  nav a { padding: 8px 14px; border-radius: 6px; font-size: var(--fs-sm);
          font-weight: 600; color: var(--text); text-decoration: none;
          transition: background .15s; }
  nav a:hover { background: var(--bg-alt); }
  .header-ctas { display: flex; align-items: center; gap: 12px; }
  .hotline { font-size: var(--fs-sm); color: var(--muted); font-weight: 600; }

  /* ══════════════════════════════════════════════
     HERO — 7/12 text + 5/12 image
  ══════════════════════════════════════════════ */
  #hero {
    background: var(--bg-alt);
    padding-block: 0;
    min-height: 560px;
    display: flex; align-items: stretch;
  }
  .hero-inner {
    display: grid;
    grid-template-columns: 7fr 5fr;
    min-height: 560px;
    max-width: var(--max);
    margin-inline: auto;
    padding-inline: var(--pad);
    gap: var(--gap);
  }
  .hero-text {
    display: flex; flex-direction: column; justify-content: center;
    padding-block: 64px; gap: 20px;
  }
  .hero-badge {
    display: inline-flex; align-items: center; gap: 6px;
    background: var(--brand); color: #fff;
    padding: 4px 12px; border-radius: 20px;
    font-size: var(--fs-xs); font-weight: 700;
    width: fit-content;
  }
  .hero-ctas { display: flex; gap: 12px; flex-wrap: wrap; }
  .hero-meta { display: flex; gap: 24px; }
  .hero-meta-item { }
  .hero-meta-item .num { font-size: var(--fs-xl); font-weight: 800; color: var(--brand); }
  .hero-meta-item .desc { font-size: var(--fs-xs); color: var(--muted); }
  .hero-visual {
    position: relative; display: flex; align-items: stretch;
  }
  .hero-visual .img-ph { border-radius: 0; height: 100%; min-height: 560px; }
  .hero-tag {
    position: absolute; bottom: 24px; left: 24px;
    background: var(--white); border-radius: 10px; padding: 10px 16px;
    box-shadow: 0 4px 16px rgba(0,0,0,.12);
    font-size: var(--fs-sm); font-weight: 700;
  }

  /* ══════════════════════════════════════════════
     GALLERY — 3 cols mosaic, ratio 3:4
  ══════════════════════════════════════════════ */
  #gallery { background: var(--white); }
  .gallery-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    grid-template-rows: auto auto;
    gap: 12px;
  }
  .gallery-grid .img-ph { border-radius: var(--radius); }
  /* first photo spans 2 rows = visual anchor */
  .gal-feature { grid-row: span 2; }
  .gal-feature .img-ph { height: 100%; min-height: 360px; aspect-ratio: unset; }

  /* ══════════════════════════════════════════════
     MENU — tab filter + 2-col list card
  ══════════════════════════════════════════════ */
  #menu { background: var(--bg); }
  .section-header { text-align: center; margin-bottom: 40px; }
  .section-header .subtext { margin-inline: auto; margin-top: 12px; }

  .tab-bar { display: flex; gap: 8px; flex-wrap: wrap; justify-content: center; margin-bottom: 32px; }
  .tab { padding: 8px 20px; border-radius: 24px; font-size: var(--fs-sm);
         font-weight: 600; background: var(--bg-alt); border: none; cursor: pointer; }
  .tab.active { background: var(--brand); color: #fff; }

  /* featured card — 1 card lớn 6/12 */
  .menu-layout { display: grid; grid-template-columns: 5fr 7fr; gap: var(--gap); align-items: start; }
  .card-featured {
    background: var(--white); border-radius: var(--radius); overflow: hidden;
    box-shadow: 0 4px 20px rgba(0,0,0,.08);
  }
  .card-featured-body { padding: 20px; }
  .card-featured-body .h3 { margin-bottom: 6px; }
  .card-featured-body .subtext { font-size: var(--fs-sm); margin-bottom: 12px;
                                  display: -webkit-box; -webkit-line-clamp: 2;
                                  -webkit-box-orient: vertical; overflow: hidden; }
  .price { font-size: var(--fs-xl); font-weight: 800; color: var(--brand); }

  /* small card grid 2×4 */
  .card-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 16px; }
  .card-sm {
    display: flex; align-items: center; gap: 12px;
    background: var(--white); border-radius: var(--radius); padding: